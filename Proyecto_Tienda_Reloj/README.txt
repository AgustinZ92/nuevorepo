# Figma → HTML/CSS (solo visual, sin JS)
Cuatro ventanas listas para copiar/pegar:

- `home.html` + `home.css` — Portada + grid de productos
- `detail.html` + `detail.css` — Detalle de producto
- `cart.html` + `cart.css` — Carrito (visual)
- `checkout.html` + `checkout.css` — Checkout (visual)

Abrí `home.html` en tu navegador. Todas las páginas usan componentes puros de HTML y CSS, sin ninguna lógica.
Podés conectar la lógica con tu backend después (fetch/axios, formularios, etc.).
